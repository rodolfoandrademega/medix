"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "../../lib/supabase";
import "./auth.css";
export default function AuthPage(){const [mode,setMode]=useState<"login"|"signup">("login"),[fullName,setFullName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[feedback,setFeedback]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setFeedback("");if(!supabase)return setFeedback("A conexão com o Supabase ainda não foi configurada.");setLoading(true);if(mode==="signup"){const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:fullName},emailRedirectTo:`${window.location.origin}/onboarding`}});if(error)setFeedback(error.message);else if(!data.session)setFeedback("Conta criada. Confira seu e-mail para confirmar o acesso e depois entre na Medix.");else router.push("/onboarding")}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setFeedback("E-mail ou senha não conferem. Tente novamente.");else router.push("/onboarding")}setLoading(false)}return <main className="auth-page"><section className="auth-aside"><Link href="/" className="brand"><span className="brand-mark">M</span> medix</Link><div><div className="eyebrow">Cuidado que evolui</div><h1>Uma nova rotina<br/>começa <em>aqui.</em></h1><p>Mais organização para sua equipe, mais atenção para cada paciente.</p></div><div className="auth-quote">“A Medix nos trouxe a clareza que faltava para crescer.”<small>Dra. Carolina Mendes · Instituto Vitta</small></div></section><section className="auth-form-area"><Link className="back-home" href="/">← Voltar para o site</Link><div className="auth-card"><div className="auth-tabs"><button className={mode==="login"?"active":""} onClick={()=>{setMode("login");setFeedback("")}}>Entrar</button><button className={mode==="signup"?"active":""} onClick={()=>{setMode("signup");setFeedback("")}}>Criar conta</button></div><h2>{mode==="login"?"Que bom ver você.":"Vamos começar."}</h2><p>{mode==="login"?"Entre para acessar a sua clínica.":"Crie sua conta para configurar sua clínica."}</p><form onSubmit={submit}>{mode==="signup"&&<label>Seu nome<input value={fullName} onChange={e=>setFullName(e.target.value)} required placeholder="Como podemos te chamar?"/></label>}<label>E-mail profissional<input value={email} onChange={e=>setEmail(e.target.value)} required type="email" placeholder="voce@clinica.com.br"/></label><label>Senha<input value={password} onChange={e=>setPassword(e.target.value)} required minLength={6} type="password" placeholder="Mínimo de 6 caracteres"/></label>{mode==="login"&&<button type="button" className="forgot" onClick={()=>setFeedback("A recuperação de senha será disponibilizada na próxima etapa.")}>Esqueci minha senha</button>}{feedback&&<div className="auth-feedback">{feedback}</div>}<button disabled={loading} className="button auth-submit">{loading?"Aguarde...":mode==="login"?"Entrar na Medix":"Criar minha conta"}<span>→</span></button></form><small className="terms">Ao continuar, você concorda com os Termos de uso e a Política de privacidade da Medix.</small></div></section></main>}
